[package]
name = "pmix"
version = "0.1.0"
edition = "2024"
description = "Low level Rust bindings"
license = "BSD"

[build-dependencies]
bindgen = "0.72.1"

[dependencies]
cstring-array = "0.1"
libc = "0.2"

[dependencies.bitflags]
version = "2.11.0"

[dev-dependencies]
static_assertions = "1"
